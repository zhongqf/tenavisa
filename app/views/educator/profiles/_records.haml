%table
  %tr
    - Element.display_columns.each do |column|
      %th= column.title_cn
    %th Status
    %th
    %th
  - profiles.each do |profile|
    = render 'educator/profiles/record', :profile => profile