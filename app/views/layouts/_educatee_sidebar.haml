%ul.menu
  %li= link_to "My Dashboard", educatee_dashboard_path
  - if [:new, :denied].include?(@profile.aasm_current_state)
    %li Edit My Profile
    %ul.submenu
      - Page.all.each do |page|
        %li= link_to("Edit #{page.title}", edit_educatee_profile_path(:page => page))
.view.fontWhite= link_to ">> View My Profile", educatee_profile_path