/ <%
/ 
/   attachment = attribute.split('_file_name').first
/ 
/   if !@item.send(attribute).blank?
/     label_text << <<-HTML
/  <small>#{link_to _("Remove file"), { :action => 'detach', 
/                                       :id => @item, 
/                                       :attachment => attachment }, 
/                                       :confirm => _("Are you sure?")}</small>
/     HTML
/   end
/ 
/ %>
/ 
/ <li>
/   <%= form.label attribute, label_text %>
/   <%= form.file_field attachment, options %>
/   <%= typus_preview(@item, attribute) %>
/ </li>
/
- filepath = profile.send(:"#{element.key}")

%tr
  %td= form.label element.key, element.title_cn
  %td
    - if filepath.nil? || filepath.blank?
      No file uploaded yet.
    - else
      = image_tag(profile.send(:"#{element.key}"), :size => "100x135")
    %br/
    = form.file_field element.key, options
